import { EdgeComputingClient } from "@/components/screens/EdgeComputingClient";

export const metadata = {
    title: "Edge Computing y Visión Artificial | SDI Tecnología",
    description: "Procesamiento de IA de baja latencia en el borde. Visión artificial urbana, LPR y privacidad total de datos on-premise.",
};

export default function EdgeComputingPage() {
    return <EdgeComputingClient />;
}
