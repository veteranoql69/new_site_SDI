version: '3.8'

services:
  sdi-site:
    image: ghcr.io/veteranoql69/new_site_sdi:v1
    ports:
      - "4000:3000"
    environment:
      - NODE_ENV=production
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
